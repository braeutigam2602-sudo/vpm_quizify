name: Asset Pipeline (Themes)
on:
  workflow_dispatch:
    inputs:
      theme:
        description: 'Theme Name'
        required: true
        default: 'jackpot_ps5'
jobs:
  generate-assets:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo 'Generating Assets...'
